{% extends 'base.html.twig' %}

{% block Contenu %}
<table class="table table-striped table-hover">
    <thead>
        <tr>
        <th scope="col">Stage</th>
        <th scope="col">Nom</th>
        <th scope="col">URL</th>
        <th scope="col">Plus d'informations</th>
        </tr>
    </thead>
    {% for entreprise in entreprises %}
        <tbody>
            <tr>
                <th scope="row">{{entreprise.id}}</th>
                <td>{{entreprise.nom}}</td>
                <td>{{entreprise.urlSite}}</td>
                <td><a class="stage" href="{{path('entrepriseStages',{id:entreprise.id})}}">Voir</a></td>
            </tr>
        </tbody>
    {% endfor %}

</table>

<!--gpeyrelongue_sym pour se connecter a la base de donnée-->
{% endblock %}

